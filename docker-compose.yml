version: '3'

services:
  vanilla-frontend:
    build:
      context: .
    container_name: vanilla-frontend
    volumes:
      - ./:/app
    ports:
      - 3000:3000
    environment:
      - REDIS_URL=redis:6379
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - 6379:6379
  hardhat:
    build:
      context: ./contracts
    container_name: hardhat
    ports:
      - 8545:8545
